# AI 智能面试系统 · 求职者端 PRD（产品需求文档）

> **版本**：V1.0
> **日期**：2026-09-04
> **适用阶段**：PC 端 Web 应用（第一版本）
> **文档状态**：待评审
> **配套文档**：[AI面试系统PRD.md](./AI面试系统PRD.md)（企业端，B 端）

---

## 一、产品概述

### 1.1 产品名称
**AI 面试助手（Candidate Side）**

### 1.2 产品定位
面向求职者（C 端候选人）的 AI 面试陪伴产品。与企业端（B 端）共用同一套面试引擎与评估能力，求职者端承担两类入口：
1. **被动入口**：收到企业 HR 发起的异步面试邀请，凭 Token 进入专属面试页独立完成 AI 文字面试。
2. **主动入口**：求职者注册后自主选择岗位/题库进行 AI 模拟面试练习，获取能力雷达图与改进建议，沉淀个人能力档案。

### 1.3 产品愿景
让每一位求职者都能拥有一位随时可练、可量化、可复盘的 AI 面试教练——把企业级面试能力平等地赋能给每个候选人。

### 1.4 核心价值主张
- **可练**：内置覆盖主流岗位的题库与 AI 多轮追问，求职者可随时发起一场 30 分钟模拟面试。
- **可量化**：每场面试产出能力雷达图与逐题点评，能力短板一目了然。
- **可成长**：个人能力档案长期沉淀，跨场次能力曲线追踪，帮助求职者定向提升。
- **零门槛**：无需企业邀请即可注册使用模拟练习；收到企业邀请则凭链接免注册完成正式面试。

### 1.5 与企业端的关系
| 维度 | 说明 |
| --- | --- |
| 共享后端 | 复用企业端的面试引擎、题库中心、评估报告生成、LLM 网关，不重复造轮子 |
| 独立前端 | 求职者端是独立 Web 应用，独立路由、独立 UI，不与企业端后台耦合 |
| 数据隔离 | 求职者正式面试数据归属企业，模拟练习数据归属求职者；正式面试报告对求职者可见性由企业控制 |
| 账号体系 | 求职者账号独立于企业用户体系；正式异步面试凭一次性 Token 免登录访问 |

---

## 二、目标用户与场景

### 2.1 目标用户

| 角色 | 说明 | 核心诉求 |
| --- | --- | --- |
| **被动候选人** | 收到企业 HR 异步面试邀请的求职者 | 凭链接完成面试、了解流程、稳定发挥 |
| **主动求职者** | 自主注册进行模拟练习的求职者 | 随时练习、获取反馈、定向提升能力 |
| **校招/社招候选人** | 准备进入新岗位的求职者 | 真实场景模拟、岗位匹配度自测 |

> 第一版本仅 PC 端 Web，响应式适配桌面分辨率（≥1280px）。移动端在 V2 规划。

### 2.2 核心使用场景

**场景一：被动接受企业面试邀请（异步面试）**
求职者收到企业 HR 发送的面试邀请邮件/短信，点击链接后凭一次性 Token 免登录进入专属面试页。阅读说明 → 测试环境 → 在预约窗口内独立完成 AI 多轮文字问答 → 提交结束 → 查看感谢页。HR 事后在企业端查看报告与作答原文。

**场景二：主动发起模拟面试练习**
求职者注册登录后，选择目标岗位（如"高级前端工程师"）与重点维度（如"专业技能、项目经验"），系统基于岗位题库生成模拟题库并启动 AI 面试。结束后求职者获取个人能力雷达图、逐题点评与改进建议。

**场景三：能力档案与成长追踪**
求职者在个人中心查看历次模拟面试报告，叠加雷达图对比能力变化，识别短板后定向发起专项练习（如"只练项目复盘类题目"），形成"练习→反馈→再练习"闭环。

**场景四：企业面试结果反馈**
若企业 HR 开放报告可见性，求职者可在求职者端查看企业面试的评估报告（能力维度、综合评语、改进建议），但不可见 HR 内部决策结论（如是否通过）。

---

## 三、系统架构与技术选型

### 3.1 总体架构
求职者端前端独立部署，与企业端共享后端服务与 LLM 网关。

```
┌──────────────────────────────────────────────────┐
│        求职者端 Web 前端（Vue3）独立部署            │
│  Naive UI / Pinia / Vite / WebSocket 客户端        │
│  ├─ 模拟练习入口（注册登录态）                      │
│  └─ 异步面试入口（Token 免登录）                    │
└──────────────────┬───────────────────────────────┘
                   │ HTTP REST + WebSocket
┌──────────────────▼───────────────────────────────┐
│          共享后端服务（Python + FastAPI）           │
│  ┌─────────────┐ ┌──────────┐ ┌──────────────┐   │
│  │候选人侧 API  │ │ 面试引擎 │ │ 评估报告服务 │   │
│  └─────────────┘ └──────────┘ └──────────────┘   │
│  ┌────────────────────────────────────────┐     │
│  │   LLM 网关（对接大模型服务）              │     │
│  └────────────────────────────────────────┘     │
└──────────────────┬───────────────────────────────┘
                   │
      ┌────────────┼─────────────┐
      ▼            ▼             ▼
┌──────────┐ ┌─────────┐   ┌──────────┐
│  数据库   │ │对象存储  │   │ 向量库    │
│ MySQL    │ │简历/报告 │   │题库检索   │
└──────────┘ └─────────┘ └──────────┘
```

### 3.2 技术选型

| 层级 | 技术 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + Pinia + Naive UI | 与企业端一致，便于组件复用与团队协作 |
| 实时通信 | WebSocket | AI 多轮对话流式回复 |
| 后端 | Python 3.11 + FastAPI | 复用企业端后端服务 |
| 数据库 | MySQL 8.x | 求职者账号、模拟练习记录持久化 |
| 向量库 | Milvus / Chroma | 题库语义检索（复用） |
| 对象存储 | MinIO / OSS | 模拟报告附件 |
| LLM | 接入主流大模型 | 与企业端共享 LLM 网关 |

### 3.3 与企业端的边界

| 能力 | 来源 | 说明 |
| --- | --- | --- |
| 面试引擎（多轮问答、动态追问、流式输出） | 复用企业端 | 同一套引擎，不重复开发 |
| 题库中心 | 复用企业端 | 求职者练习时按岗位调取公共题库 |
| 评估报告生成 | 复用企业端 | 同一套评分模型与雷达图 |
| 异步面试 Token 鉴权 | 新增 | 候选人侧独立中间件 |
| 求职者账号体系 | 新增 | C 端独立注册登录 |
| 个人能力档案 | 新增 | 求职者跨场次能力沉淀 |
| 模拟练习记录 | 新增 | 与企业正式面试数据隔离 |

### 3.4 第二版本规划（移动端）
- V1 仅 PC 端 Web，响应式桌面分辨率（≥1280px）。
- V2 规划：移动端 H5 + 小程序，重点支持移动端完成异步面试与碎片化练习。

---

## 四、功能模块详细设计

### 4.1 功能架构总览

```
AI 面试助手（求职者端）
├── 1. 账号与登录
│   ├── 注册 / 登录
│   ├── Token 免登录入口（异步面试）
│   └── 个人资料
├── 2. 异步面试（被动入口）
│   ├── 邀请链接进入
│   ├── 面试准备页（说明 / 环境检测）
│   ├── 面试房间（AI 多轮对话）
│   └── 结束感谢页
├── 3. 模拟面试（主动入口）
│   ├── 岗位选择
│   ├── 练习配置（维度 / 时长 / 题量）
│   ├── AI 模拟面试房间
│   └── 即时能力反馈
├── 4. 我的面试
│   ├── 历史面试列表
│   ├── 面试记录回看
│   └── 能力报告查看
├── 5. 能力档案
│   ├── 能力雷达图叠加对比
│   ├── 能力成长曲线
│   └── 短板诊断与练习推荐
└── 6. 设置与隐私
    ├── 数据授权管理
    ├── 简历管理
    └── 账号注销
```

### 4.2 模块一：账号与登录

#### 4.2.1 注册 / 登录
- 支持手机号 / 邮箱注册，验证码 + 密码双因子。
- 第三方登录（微信 / GitHub）V2 接入。
- 登录态基于 JWT，单设备优先，多端互踢可配置。
- 注册即同意《求职者隐私协议》与《AI 面试数据授权说明》。

#### 4.2.2 Token 免登录入口（异步面试）
- 企业 HR 发起的异步面试生成**一次性访问 Token**（JWT，含候选人 ID、面试 ID、有效期）。
- 候选人点击邀请链接 → 后端校验 Token → 通过则免登录直接进入面试页。
- Token 单设备绑定（首次访问记录设备指纹，防止转交）。
- 链接过期前 24 小时自动邮件提醒。
- Token 失效后引导候选人联系企业 HR 重新发起。

#### 4.2.3 个人资料
- 基本信息：姓名、联系方式、求职意向（岗位 / 城市 / 薪资）。
- 简历上传与解析（复用企业端简历解析能力，解析结果归候选人所有）。
- 求职意向用于模拟练习时智能推荐岗位题库。

### 4.3 模块二：异步面试（被动入口）

> 对应企业端 PRD 11.2 节"异步面试方案"，本节从候选人视角详述体验。

#### 4.3.1 邀请链接进入
- 候选人点击邮件/短信中的链接 → 进入 Token 校验页。
- 校验通过：展示企业品牌信息、岗位名称、面试时长、有效期窗口。
- 校验失败：明确提示原因（过期 / 已使用 / 设备不符）并给出联系 HR 入口。

#### 4.3.2 面试准备页
进入正式面试前强制完成以下步骤，降低因环境问题导致的作答失败：

| 步骤 | 内容 |
| --- | --- |
| 阅读说明 | 面试流程、时长规则、暂停规则、防作弊提示、AI 面试官说明 |
| 环境检测 | 浏览器兼容性、网络延迟、输入法可用性、麦克风（可选） |
| 设备确认 | 记录设备指纹（Token 单设备绑定） |
| 知情同意 | 勾选同意《面试作答数据授权》后才能开始 |

#### 4.3.3 面试房间（候选人视角）
- **顶部**：剩余时长倒计时、暂停按钮、结束按钮。
- **对话区**：AI 多轮文字问答，AI 回复流式输出。
- **右侧进度面板**：已答题数 / 总题数、当前考察维度、已用时长。
- **行为规则**：
  - 允许暂停，累计暂停次数 ≤ N 次（HR 配置），总时长扣除暂停时长。
  - 倒计时归零自动提交当前作答。
  - 离开页面（失焦）次数与时长记录，写入报告。
  - 禁止复制粘贴题目内容。
- **离线保护**：网络中断时本地缓存作答内容，恢复后自动同步。

#### 4.3.4 结束感谢页
- 感谢文案 + 预计反馈时间（HR 配置）。
- 若企业开放报告可见：预告报告生成后可查看。
- 引导注册（若未注册）：可将本次面试关联到个人档案，便于后续回看。
- 不强制注册，保护候选人体验。

### 4.4 模块三：模拟面试（主动入口）

#### 4.4.1 岗位选择
- 内置覆盖主流岗位的公共题库（前端、后端、产品、数据、设计等）。
- 支持按岗位大类、难度、技能标签筛选。
- 候选人上传简历后，AI 智能推荐匹配岗位题库。
- 支持自定义岗位（V2）：候选人粘贴 JD，AI 自动生成题库。

#### 4.4.2 练习配置
| 配置项 | 说明 |
| --- | --- |
| 重点维度 | 多选：专业技能 / 项目经验 / 逻辑思维 / 沟通表达 / 学习能力 / 文化匹配 |
| 面试时长 | 15 / 30 / 45 / 60 分钟可选，默认 30 分钟 |
| 题量 | AI 根据时长推荐 8-10 题，可手动调整 5-15 题 |
| 难度 | 简单 / 中等 / 困难，默认根据岗位难度自动匹配 |
| 追问深度 | 每题最多 0 / 1 / 2 次追问，默认 1 次 |

#### 4.4.3 AI 模拟面试房间
- 复用企业端 AI 自主面试模式的对话引擎与流式输出。
- AI 行为规则同企业端 PRD 4.4.2：开场 → 多轮问答 → 动态追问 → 收尾反馈。
- 候选人视角差异：
  - 无面试官旁听，纯 AI 对话。
  - 不强制防作弊（模拟场景放宽）。
  - 支持随时"跳过本题"与"提前结束"。
- 结束后即时生成能力报告。

#### 4.4.4 即时能力反馈
- 面试结束 30s 内生成模拟报告。
- 报告内容与企业端评估报告一致（雷达图、逐题点评、改进建议）。
- 候选人可立即查看，无需等待人工审核。
- 报告归入个人能力档案。

### 4.5 模块四：我的面试

#### 4.5.1 历史面试列表
- 统一展示：模拟练习 + 企业异步面试（Token 关联后）。
- 列表字段：类型（模拟 / 企业）、岗位、日期、综合分、推荐等级（仅模拟）。
- 筛选：按类型、岗位、时间段。
- 排序：默认按时间倒序。

#### 4.5.2 面试记录回看
- 全量对话记录（含时间戳、发言人）。
- 每题作答原文 + AI 点评 + 参考答案要点。
- 支持导出（PDF，V2）。
- 模拟练习记录永久保留；企业面试记录保留期由企业策略控制。

#### 4.5.3 能力报告查看
- **模拟练习报告**：完整可见（雷达图、逐题分析、改进建议）。
- **企业面试报告**：可见性由企业控制（见 7.3 数据可见性策略）。
  - 完全开放：候选人查看完整报告。
  - 摘要开放：仅雷达图与综合评语，不见逐题原文。
  - 完全关闭：仅显示"已完成"，引导联系企业。

### 4.6 模块五：能力档案

#### 4.6.1 能力雷达图叠加对比
- 选择 2-5 场面试报告，雷达图叠加展示能力维度变化。
- 高亮短板维度（低于 60 分）与优势维度（高于 85 分）。
- 维度附 AI 评分依据（对应题目与作答摘要），可溯源。

#### 4.6.2 能力成长曲线
- 单维度按时间轴展示分数变化曲线（如"专业技能"近 3 个月趋势）。
- 标注关键节点：首次练习、突破 80 分、连续提升等。
- 支持导出成长曲线图为图片（V2）。

#### 4.6.3 短板诊断与练习推荐
- AI 综合分析历史报告，识别 Top 3 能力短板。
- 推荐定向练习方案：如"项目复盘类题目专项练习 3 场"。
- 一键发起专项练习，题库自动筛选对应维度题目。
- 形成"诊断 → 练习 → 再诊断"闭环。

### 4.7 模块六：设置与隐私

#### 4.7.1 数据授权管理
- 候选人可查看所有数据授权记录：哪些企业发起了面试、使用了简历、生成了报告。
- 支持撤回授权（撤回后企业不可再访问候选人新增数据，已生成报告保留权属企业）。
- 数据保留期可配置（候选人可设置简历保留期，到期自动删除）。

#### 4.7.2 简历管理
- 多版本简历管理（如"前端版""全栈版"）。
- 模拟练习时可选择使用哪版简历作为出题依据。
- 企业面试时简历由企业提供，候选人不主动上传。

#### 4.7.3 账号注销
- 一键注销账号，注销前确认数据归属处理方式。
- 注销后：模拟练习数据删除，企业面试数据按企业策略保留。

---

## 五、详细业务流程

### 5.1 被动面试流程（异步面试）

```
[企业 HR 创建异步面试任务] ──► [发送邀请链接]
     │
     ▼
[候选人点击链接] ──► [Token 校验]
     │
     ▼
[面试准备页（说明 / 环境检测 / 知情同意）]
     │
     ▼
[候选人进入面试房间] ──► [AI 多轮文字问答] ──► [动态追问]
     │  （可暂停，总时长受控）
     ▼
[候选人提交 / 超时自动收尾]
     │
     ▼
[AI 生成评估报告] ──► [通知 HR 查看]
     │
     ▼
[感谢页] ──► [引导注册关联档案（可选）]
```

### 5.2 主动练习流程（模拟面试）

```
[求职者注册登录] ──► [选择目标岗位]
     │
     ▼
[配置练习（维度 / 时长 / 题量 / 难度）]
     │
     ▼
[AI 生成模拟题库] ──► [候选人校对题目（可选）]
     │
     ▼
[启动 AI 模拟面试房间] ──► [AI 多轮问答] ──► [动态追问]
     │  （可跳题 / 提前结束）
     ▼
[面试结束] ──► [30s 内生成模拟报告]
     │
     ▼
[查看能力雷达图 + 逐题点评 + 改进建议]
     │
     ▼
[归入个人能力档案] ──► [短板诊断] ──► [推荐专项练习]
```

### 5.3 状态流转

**异步面试会话状态（候选人侧）：**
```
未进入 ──Token校验──► 准备中 ──开始──► 进行中 ──提交/超时──► 已完成
                              │                              │
                              └──已暂停──► 进行中             └──已过期（Token失效）
```

**模拟练习会话状态：**
```
配置中 ──启动──► 进行中 ──提交/跳完──► 已完成 ──生成报告──► 已归档
```

---

## 六、数据模型（核心实体）

> 仅列出求职者端新增/扩展实体，与企业端共享实体见企业端 PRD 第六章。

| 实体 | 关键字段 |
| --- | --- |
| **求职者账号（CandidateAccount）** | id, 手机/邮箱, 密码哈希, 注册时间, 最后登录, 设备指纹 |
| **求职者档案（CandidateProfile）** | account_id, 姓名, 求职意向JSON, 简历版本[] |
| **异步面试任务（AsyncInterviewTask）** | id, interview_id, token, 有效期起止, 单次时长, 暂停次数上限, 状态, 候选人邮箱 |
| **作答会话（AnswerSession）** | id, task_id, 实际开始时间, 累计暂停时长, 提交时间, 设备指纹 |
| **作答日志（AnswerEvent）** | id, session_id, 事件类型(失焦/恢复/切屏/提交), 时间戳, 元数据 |
| **模拟练习（MockInterview）** | id, account_id, 岗位id, 配置JSON, 题库id, 报告id, 创建时间 |
| **能力档案（AbilityProfile）** | account_id, 雷达图聚合JSON, 短板诊断JSON, 更新时间 |
| **数据授权（DataConsent）** | id, account_id, 企业org_id, 授权类型, 授权时间, 撤回时间, 状态 |

### 6.1 与企业端实体的关联
- `AsyncInterviewTask.interview_id` 关联企业端 `Interview.id`。
- `MockInterview.报告id` 关联企业端 `Report.id`（模拟练习复用报告生成）。
- `DataConsent.企业org_id` 关联企业端 `Organization.id`。

---

## 七、非功能需求

| 维度 | 要求 |
| --- | --- |
| **性能** | AI 单次回复首字延迟 ≤ 2s；页面首屏加载 ≤ 2s；支持 500 并发求职者会话 |
| **可用性** | 系统可用性 99.5%；LLM 调用失败自动降级重试；离线本地缓存兜底 |
| **安全** | Token 单设备绑定 + 时效校验；简历加密存储；HTTPS 传输；防作弊日志留存 |
| **合规** | 候选人简历使用需授权；数据保留期可配置；支持数据导出与删除（GDPR 友好）；授权撤回机制 |
| **兼容** | Chrome / Edge / Firefox / Safari 最新两个大版本；分辨率 ≥1280px |
| **可扩展** | LLM 网关支持多模型切换；题库与报告模型支持自定义维度；移动端 V2 预留 |
| **体验** | 异步面试准备页强制环境检测，降低作答失败率；离线保护防数据丢失 |

### 7.3 数据可见性策略（关键）

| 数据类型 | 求职者可见性 | 控制方 |
| --- | --- | --- |
| 模拟练习报告 | 完整可见 | 求职者本人 |
| 模拟练习对话原文 | 完整可见 | 求职者本人 |
| 企业异步面试报告 | 可见性由企业配置（完全开放/摘要开放/完全关闭） | 企业 HR |
| 企业异步面试对话原文 | 默认不可见，企业可开放 | 企业 HR |
| 企业决策结论（通过/淘汰） | 不可见，由企业主动通知 | 企业 HR |
| 候选人简历（企业上传） | 不可见（企业上传的简历权属企业） | 企业 |
| 候选人简历（本人上传用于模拟） | 完整可见 | 求职者本人 |

---

## 八、项目里程碑

| 阶段 | 交付内容 |
| --- | --- |
| **C-M1：被动入口底座** | Token 鉴权、面试准备页、异步面试房间、结束感谢页 |
| **C-M2：主动练习** | 注册登录、岗位选择、练习配置、AI 模拟面试、即时报告 |
| **C-M3：能力档案** | 历史面试列表、报告回看、雷达图叠加、成长曲线、短板诊断 |
| **C-M4：隐私与体验** | 数据授权管理、简历多版本、离线保护、防作弊日志、账号注销 |

> 具体排期由项目管理阶段结合资源确定，本 PRD 不含时间承诺。

---

## 九、风险与依赖

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 异步面试 Token 被转交 | 作答不真实 | 单设备绑定 + 设备指纹 + 失焦日志 |
| 候选人离线导致作答丢失 | 体验差、数据丢失 | 本地缓存 + 自动同步 + 离线提示 |
| 模拟练习 LLM 成本 | 运营成本上升 | 模拟练习限频（如每日 3 场）；分级调用模型；题库缓存 |
| 企业报告可见性配置不当 | 候选人体验/合规风险 | 默认完全关闭，企业显式开放；审计日志记录开放行为 |
| 求职者与企业数据混淆 | 合规风险 | 数据隔离设计；账号体系独立；授权机制 |
| 简历解析准确率（模拟出题） | 出题质量下降 | 支持人工校对；解析模型持续训练 |

### 9.1 对企业端的依赖
| 依赖项 | 说明 |
| --- | --- |
| 面试引擎 | 多轮问答、动态追问、流式输出能力必须就绪（企业端 M2） |
| 题库中心 | 模拟练习依赖岗位题库（企业端 M2） |
| 评估报告生成 | 模拟练习报告复用（企业端 M3） |
| 异步面试能力 | 被动入口依赖企业端 V2 异步方案（企业端 PRD 11.2） |

---

## 十、附录

### 10.1 术语表
- **异步面试**：候选人无需与 AI/HR 同时在线，在预约窗口内独立完成面试。
- **模拟面试**：求职者主动发起的 AI 练习面试，无企业参与。
- **能力雷达图**：多维能力可视化图表，与企业端一致。
- **Token 免登录**：企业发起异步面试时生成的一次性访问凭证。
- **设备指纹**：识别候选人访问设备的特征串，用于防转交。

### 10.2 与企业端 PRD 的章节对应
| 求职者端章节 | 对应企业端章节 |
| --- | --- |
| 4.3 异步面试 | 企业端 11.2 异步面试方案 |
| 4.4 模拟面试 | 企业端 4.4.2 AI 自主面试模式 |
| 4.5 能力报告 | 企业端 4.6 评估报告 |
| 6 数据模型 | 企业端 6 数据模型（扩展） |

### 10.3 待评审问题
1. 模拟练习是否收费？建议 V1 免费 + 每日限频，V2 推出会员增值服务。
2. 求职者端是否独立品牌？建议 V1 共用主品牌"AI Interview Pro"，V2 评估独立子品牌。
3. 企业异步面试报告默认可见性？建议默认完全关闭，由企业显式开放。
4. 求职者上传的简历是否可用于企业端出题？建议默认仅用于本人模拟，企业面试用企业上传简历。

---

**文档结束。**
